{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/abap2UI5/linter/main/data/abap2ui5lint.schema.json",
  "title": "abap2ui5lint.jsonc",
  "description": "Configuration for abap2UI5 linter (https://github.com/abap2UI5/linter)",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "URL of this schema — editor completion only"
    },
    "paths": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Files or directories to check. Used only when the CLI got no positional paths."
    },
    "ui5": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+(\\.\\d+)?$",
      "description": "The UI5 version the target system runs. Controls and members introduced later are reported; deprecations are reported once in effect at this version."
    },
    "minUi5": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+(\\.\\d+)?$",
      "description": "Alias of \"ui5\"."
    },
    "distribution": {
      "enum": [
        "sapui5",
        "openui5"
      ],
      "description": "Which distribution the target system serves. On \"openui5\", controls from SAPUI5-only libraries are reported."
    },
    "allow": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Control or control.member names accepted despite the version floor, e.g. \"sap.m.Avatar.displaySize\"."
    },
    "render": {
      "type": "boolean",
      "description": "false skips the render gate (no browser needed)."
    },
    "properties": {
      "type": "boolean",
      "description": "false skips the property gate."
    },
    "failOn": {
      "enum": [
        "hint",
        "warning",
        "error",
        "never"
      ],
      "description": "Lowest severity that fails the build. Everything is always reported — this only decides the exit code."
    },
    "baseline": {
      "type": "string",
      "description": "Path (relative to this config) of the baseline file: findings frozen at adoption time are suppressed, new findings fail, a stale entry fails too. Create/refresh it with --update-baseline."
    },
    "badge": {
      "anyOf": [
        {
          "type": "string",
          "description": "Path (relative to this config) of the shields.io endpoint JSON to write. Writes the verdict badge."
        },
        {
          "$ref": "#/definitions/badge"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/definitions/badge"
          },
          "description": "One entry per badge kind."
        }
      ],
      "description": "Write shields.io endpoint JSON for every run, so the README can show what the corpus IS (\"abap2UI5 | 148 apps · 172 views · 2,176 controls\") and what the gate said about it (\"check-abap2UI5 | 83 rules passed\")."
    },
    "rules": {
      "type": "object",
      "additionalProperties": false,
      "description": "Per rule: false to switch it off, a severity string, or { severity, exclude }.",
      "properties": {
        "aggregation-in-aggregation": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'aggregation-in-aggregation' — default severity: error"
        },
        "aggregation-too-new": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'aggregation-too-new' — default severity: error"
        },
        "attribute-without-element": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'attribute-without-element' — default severity: error"
        },
        "binding-for-event": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'binding-for-event' — default severity: error"
        },
        "binding-on-association": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'binding-on-association' — default severity: error"
        },
        "binding-to-local": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'binding-to-local' — default severity: warning"
        },
        "binding-to-nonpublic": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'binding-to-nonpublic' — default severity: error"
        },
        "binding-to-reference": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'binding-to-reference' — default severity: error"
        },
        "binding-type-mismatch": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'binding-type-mismatch' — default severity: warning"
        },
        "chain-element-per-line": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'chain-element-per-line' — default severity: hint"
        },
        "chain-house-layout": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'chain-house-layout' — default severity: hint"
        },
        "chain-indentation": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'chain-indentation' — default severity: hint"
        },
        "collapsed-brace-in-style": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'collapsed-brace-in-style' — default severity: error"
        },
        "collection-bound-to-property": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'collection-bound-to-property' — default severity: error"
        },
        "commercial-ui5-host": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'commercial-ui5-host' — default severity: warning"
        },
        "control-deprecated": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'control-deprecated' — default severity: warning"
        },
        "control-too-new": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'control-too-new' — default severity: warning"
        },
        "date-type-without-source": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'date-type-without-source' — default severity: error"
        },
        "denied-control-method": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'denied-control-method' — default severity: error"
        },
        "display-root-mismatch": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'display-root-mismatch' — default severity: error"
        },
        "duplicate-aggregation": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'duplicate-aggregation' — default severity: error"
        },
        "duplicate-for-iterator": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'duplicate-for-iterator' — default severity: warning"
        },
        "duplicate-id": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'duplicate-id' — default severity: error"
        },
        "duplicate-property": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'duplicate-property' — default severity: error"
        },
        "enum-value-too-new": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'enum-value-too-new' — default severity: warning"
        },
        "escaped-brace-in-backtick": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'escaped-brace-in-backtick' — default severity: error"
        },
        "event-arg-out-of-range": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'event-arg-out-of-range' — default severity: error"
        },
        "event-arg-unresolved": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'event-arg-unresolved' — default severity: warning"
        },
        "event-for-property": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'event-for-property' — default severity: error"
        },
        "event-on-disabled-control": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'event-on-disabled-control' — default severity: hint"
        },
        "event-parameter-too-new": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'event-parameter-too-new' — default severity: warning"
        },
        "event-without-handler": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'event-without-handler' — default severity: hint"
        },
        "excess-shut": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'excess-shut' — default severity: error"
        },
        "frontend-action-unknown-id": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'frontend-action-unknown-id' — default severity: error"
        },
        "frozen-view-builder": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'frozen-view-builder' — default severity: error"
        },
        "get-viewname-removed": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'get-viewname-removed' — default severity: error"
        },
        "hardcoded-binding-path": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'hardcoded-binding-path' — default severity: warning"
        },
        "icon-removed": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'icon-removed' — default severity: warning"
        },
        "icon-too-new": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'icon-too-new' — default severity: warning"
        },
        "invalid-action-payload": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'invalid-action-payload' — default severity: error"
        },
        "invalid-aggregation-child": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'invalid-aggregation-child' — default severity: error"
        },
        "invalid-expression-binding": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'invalid-expression-binding' — default severity: error"
        },
        "invalid-frontend-action": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'invalid-frontend-action' — default severity: error"
        },
        "invalid-keyboard-shortcut": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'invalid-keyboard-shortcut' — default severity: error"
        },
        "invalid-property-value": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'invalid-property-value' — default severity: error"
        },
        "json-bind-on-scalar-property": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'json-bind-on-scalar-property' — default severity: warning"
        },
        "json-literal-in-attribute": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'json-literal-in-attribute' — default severity: error"
        },
        "live-event-roundtrip": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'live-event-roundtrip' — default severity: hint"
        },
        "manual-init-flag": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'manual-init-flag' — default severity: warning"
        },
        "member-deprecated": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'member-deprecated' — default severity: warning"
        },
        "member-too-new": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'member-too-new' — default severity: warning"
        },
        "missing-accessibility": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'missing-accessibility' — default severity: hint"
        },
        "missing-on-navigated-branch": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'missing-on-navigated-branch' — default severity: warning"
        },
        "missing-required-aggregation": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'missing-required-aggregation' — default severity: warning"
        },
        "missing-view-display-on-navigated": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'missing-view-display-on-navigated' — default severity: error"
        },
        "non-released-api": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'non-released-api' — default severity: warning"
        },
        "obsolete-binder": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'obsolete-binder' — default severity: warning"
        },
        "obsolete-frontend-event": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'obsolete-frontend-event' — default severity: warning"
        },
        "obsolete-model-update": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'obsolete-model-update' — default severity: warning"
        },
        "popover-anchor-unknown-id": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'popover-anchor-unknown-id' — default severity: error"
        },
        "popover-display-val": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'popover-display-val' — default severity: error"
        },
        "raw-javascript-to-frontend": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'raw-javascript-to-frontend' — default severity: warning"
        },
        "relative-binding-without-context": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'relative-binding-without-context' — default severity: error"
        },
        "sapui5-only-control": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'sapui5-only-control' — default severity: error"
        },
        "separate-lifecycle-ifs": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'separate-lifecycle-ifs' — default severity: warning"
        },
        "settable-property-via-action": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'settable-property-via-action' — default severity: hint"
        },
        "source-line-too-long": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'source-line-too-long' — default severity: error"
        },
        "too-many-children": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'too-many-children' — default severity: error"
        },
        "toolbar-control-in-bar": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'toolbar-control-in-bar' — default severity: warning"
        },
        "trailing-empty-event-arg": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'trailing-empty-event-arg' — default severity: warning"
        },
        "ui5-internal-access": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'ui5-internal-access' — default severity: warning"
        },
        "unconverted-abap-boolean": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'unconverted-abap-boolean' — default severity: error"
        },
        "uncurated-formatter": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'uncurated-formatter' — default severity: error"
        },
        "undeclared-namespace": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'undeclared-namespace' — default severity: error"
        },
        "unescaped-brace-in-style": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'unescaped-brace-in-style' — default severity: error"
        },
        "unknown-aggregation": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'unknown-aggregation' — default severity: error"
        },
        "unknown-binding-path": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'unknown-binding-path' — default severity: warning"
        },
        "unknown-control": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'unknown-control' — default severity: error"
        },
        "unknown-event-parameter": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'unknown-event-parameter' — default severity: hint"
        },
        "unknown-frontend-action": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'unknown-frontend-action' — default severity: error"
        },
        "unknown-icon": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'unknown-icon' — default severity: error"
        },
        "unknown-model": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'unknown-model' — default severity: error"
        },
        "unknown-property": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'unknown-property' — default severity: error"
        },
        "unknown-view-slot": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'unknown-view-slot' — default severity: error"
        },
        "unused-public-attribute": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'unused-public-attribute' — default severity: hint"
        },
        "view-never-displayed": {
          "$ref": "#/definitions/rule",
          "description": "abap2UI5 linter rule 'view-never-displayed' — default severity: error"
        },
        "render-error": {
          "$ref": "#/definitions/rule",
          "description": "render-gate failures — false or a matching 'exclude' waives them (a waived file that renders clean is called out as stale), a severity decides what they count as. Default severity: error"
        }
      }
    }
  },
  "definitions": {
    "badge": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "file"
      ],
      "properties": {
        "kind": {
          "enum": [
            "corpus",
            "checks"
          ],
          "description": "\"corpus\" (what the repository is, blue) or \"checks\" (what the gate said, green/red). Default: \"checks\"."
        },
        "file": {
          "type": "string",
          "description": "Path (relative to this config) of the endpoint JSON to write."
        },
        "label": {
          "type": "string",
          "description": "Left half of the badge. Default: \"abap2UI5\" for corpus, \"check-abap2UI5\" for checks."
        },
        "logo": {
          "type": [
            "string",
            "null"
          ],
          "description": "simple-icons logo name, or null for none. Default: none."
        },
        "labelColor": {
          "type": "string",
          "description": "Colour of the left half. Default: shields grey \"555\"."
        }
      }
    },
    "rule": {
      "anyOf": [
        {
          "type": "boolean",
          "description": "false switches the rule off"
        },
        {
          "enum": [
            "hint",
            "warning",
            "error"
          ],
          "description": "severity override"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "severity": {
              "enum": [
                "hint",
                "warning",
                "error"
              ],
              "description": "severity override"
            },
            "exclude": {
              "type": "array",
              "items": {
                "type": "string",
                "format": "regex"
              },
              "description": "File regex patterns this rule is not applied to, case insensitive."
            }
          }
        }
      ]
    }
  }
}
