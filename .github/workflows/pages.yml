name: Rules page
# Publishes docs/ (the generated rule reference) to GitHub Pages -
# https://abap2ui5.github.io/linter/ - the same role rules.abaplint.org plays
# next door. The page is generated and committed; this only serves it.
#
# Needs Pages enabled once in the repository settings, with "GitHub Actions"
# as the source.
on:
  push:
    branches: [main]
    paths:
      - docs/**
      - .github/workflows/pages.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# one deployment at a time; a queued run supersedes a waiting one
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v5
        with:
          path: docs
      - id: deployment
        uses: actions/deploy-pages@v5
